# Copia este archivo a ".env" en el host y (si lo deseas) define NAUTA_PASS
# para no dejar la contraseña en config.toml. Docker Compose exige que ".env"
# exista aunque esté vacío.
#
# NAUTA_USERNAME=usuario@nauta.com.cu
# NAUTA_PASS=mi-clave-secreta